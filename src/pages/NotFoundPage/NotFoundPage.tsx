import { CenteredWrapper } from 'ui';
import { NotFoundContent } from './components';

export const NotFoundPage = () => {
    return (
        <CenteredWrapper>
            <NotFoundContent />
        </CenteredWrapper>
    );
};
